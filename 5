#include <stdio.h>
main ()
{
float pi, r, area;
printf("The value of pi : ");
scanf("%f", &pi);
printf("The value of r : ");
scanf("%f", &r);
area = pi * r * r;
printf("So the area will be : %f", area);
return 0;
} 
